# PatternBridge dependencies
# Python 3.10+ required
#
# This file installs everything, which is convenient when working in a clone.
# Installing the package instead lets you take only what you need:
#
#   pip install patternbridge              # geometry core, numpy only
#   pip install "patternbridge[svg]"       # + SVG output
#   pip install "patternbridge[all]"       # everything below, plus torch
#
# See pyproject.toml for the full list of extras.

# Geometry layer — the only hard requirement
numpy>=1.24.0

# Vision layer
anthropic>=0.30.0
Pillow>=10.0.0

# Output layer
svgwrite>=1.4.0
reportlab>=4.0.0

# Tools
flask>=3.0.0
requests>=2.28.0
pymupdf>=1.24.0    # pattern PDF piece extraction

# Optional: GPT-4o support
# openai>=1.0.0

# Optional: CNN classifier
# torch>=2.0.0
# torchvision>=0.15.0
